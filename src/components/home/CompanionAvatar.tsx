/**
 * Re-export — the canonical CompanionAvatar lives in src/components/ui/.
 * This shim keeps the local import in ChatSection working without a path change.
 */
export { default } from '../ui/CompanionAvatar'
